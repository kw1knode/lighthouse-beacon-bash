# Lighthouse Beacon Node


========================================================
#### **Download install.sh**
`git clone `

`cd lighthouse`

`chmod +x lighthouse_install.sh`

#### **Run install.sh**
`./lighthouse_install.sh`
